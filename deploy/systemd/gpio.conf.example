# Enables the REAL GPIO driver. Install only after the LED self-test in the
# README has passed on every channel.
[Service]
SupplementaryGroups=gpio
ExecStart=
ExecStart=/opt/dynamic-thermal-charge/venv/bin/dynamic-thermal-charge run --controller --driver gpio
